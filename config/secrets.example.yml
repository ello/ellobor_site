# Do not keep production secrets in the repository,
# instead read values from the environment.
# secret token (min 30 chars) - only needed for production - use '$ rake secret' to generate

# Add account credentials and API keys here.
# See http://railsapps.github.com/rails-environment-variables.html

development:
  secret_key_base: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

  ## general prefs
  app_url: bor.ello.dev
  base_site_title: 'Bill of Rights'
  base_company_name: 'Ello'

  ## lockup password
  lockup_codeword:

  ## postmark stuff
  postmark_key: 

  # mailcatcher, set to true to turn on in dev mode
  mail_catcher:

  ## mailchimp stuff
  mailchimp_key: xxx
  mailchimp_list_id: xxx

  ## facebook stuff
  facebook_page_id: 123456
  facebook_app_id: 123456
  facebook_app_secret: xxx

test:
  secret_key_base: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  lockup_codeword: triangles4life
